import React from 'react';
import { Container, Table } from 'react-bootstrap';

const Orders = () => {
    return (
        <Container>
            <Table>

            </Table>
        </Container>
    );
};

export default Orders;